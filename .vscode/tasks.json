{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run push_swap and checker",
            "type": "shell",
            "command": "echo '${input:ARGS}' | xargs ./push_swap | xargs -I {} ./checker ${input:ARGS}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "ARGS",
            "type": "promptString",
            "description": "Enter ARGS for push_swap and checker:"
        }
    ]
}